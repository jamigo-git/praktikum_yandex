<div class="container">
    <Form class="reg_form">
        <div class="reg_header">
            <h2>Регистрация</h2>
        </div>
        <div class="input_container">
            {{> Input placeholder="Почта" class="reg_form_input" name="email"}}
            {{> Input placeholder="Логин" class="reg_form_input" name="login"}}
            {{> Input placeholder="Имя" class="reg_form_input" name="first_name"}}
            {{> Input placeholder="Фамилия" class="reg_form_input" name="second_name"}}
            {{> Input placeholder="Телефон" class="reg_form_input" name="phone"}}
            {{> Input placeholder="Пароль" class="reg_form_input" name="password"}}
            {{> Input placeholder="Повторите пароль" class="reg_form_input" type="password" name="password"}}
        </div>
        <div class="btn_container">
            {{> Button label="Зарегистрироваться" type="primary"}}
            {{> Button label="Войти" type="secondary"}}
        </div>
    </Form>
</div>

