{{!-- <main class="container">
    <Form class="reg-form">
        <div class="reg_header">
            <h2>Регистрация</h2>
        </div>
        <div class="input-container">
            {{> Input placeholder="Почта" class="reg-form-input" name="email"}}
            {{> Input placeholder="Логин" class="reg-form-input" name="login"}}
            {{> Input placeholder="Имя" class="reg-form-input" name="first_name"}}
            {{> Input placeholder="Фамилия" class="reg-form-input" name="second_name"}}
            {{> Input placeholder="Телефон" class="reg-form-input" name="phone"}}
            {{> Input placeholder="Пароль" class="reg-form-input" name="password"}}
            {{> Input placeholder="Повторите пароль" class="reg-form-input" type="password" name="password"}}
        </div>
        <div class="btn-container">
            {{> Button label="Зарегистрироваться" type="primary"}}
            {{> Button label="Войти" type="secondary"}}
        </div>
    </Form>
</main>
 --}}
