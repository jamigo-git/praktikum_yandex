<div class="container">
    <Form class="reg_form">
        <div class="reg_header">
            <h2>Регистрация</h2>
        </div>
        <div class="input_container">
            {{> Input placeholder='Почта' class='reg_form_input'}}
            {{> Input placeholder='Логин' class='reg_form_input'}}
            {{> Input placeholder='Имя' class='reg_form_input'}}
            {{> Input placeholder='Фамилия' class='reg_form_input'}}
            {{> Input placeholder='Телефон' class='reg_form_input'}}
            {{> Input placeholder='Пароль' class='reg_form_input'}}
            {{> Input placeholder='Повторите пароль' class='reg_form_input' type='password'}}
        </div>
        <div class="btn_container">
            {{> Button label='Зарегистрироваться' type="primary"}}
            {{> Button label='Войти' type="secondary"}}
        </div>
    </Form>
</div>

