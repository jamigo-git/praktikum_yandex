{{!-- <main class="profile-edit-container">
    {{> ButtonNav class="button-back"}}
    <Form class="profile-edit-form">
        {{> Avatar label="jamigo" class="avatar-profile"}}
        <div class="profile-edit-input-container">
            {{> Input label="Имя" placeholder="Игорь" class="profile-edit-input" name="first_name"}}
            {{> Input label="Фамилия" placeholder="Ямалеев" class="profile-edit-input" name="second_name"}}
            {{> Input label="Логин" placeholder="jamigo" class="profile-edit-input" name="login"}}
            {{> Input label="Почта" placeholder="jamigo@ya.ru" class="profile-edit-input" name="email"}}
            {{> Input label="Телефон" placeholder="+7 (917) 207 90 22" class="profile-edit-input" name="phone"}}
        </div>
        <div class="profile-edit-btn-container">
            {{> Button label="Сохранить" type="primary"}}
            {{> Button label="Выйти" type="secondary"}}
        </div>
    </Form>
</main> --}}

