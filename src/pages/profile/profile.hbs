<div class="container">
    <Form class="profile_form">
        {{!-- {{> Avatar label='Jamigo' avatar='123' }} --}}
        <img src="{{avatar}}" class="avatar_image">{{label}}</span>
        <div class="input_container">
            {{> FormString label='Имя' value='Игорь' }}
            {{> FormString label='Фамилия' value='Ямалеев' }}
            {{> FormString label='Логин' value='jamigo' }}
            {{> FormString label='Почта' value='jamigo@ya.ru' }}
            {{> FormString label='Телефон' value='+7 (917) 207 90 22' }}
        </div>
        <div class="btn_container">
            {{> Button label='Изменить данные' type="secondary"}}
            {{> Button label='Изменить пароль' type="secondary"}}
            {{> Button label='Выйти' type="secondary"}}
        </div>
    </Form>
</div>

