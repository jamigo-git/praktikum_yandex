<div class="avatar__container">
    <div>
        {{#if avatar}}
            <img src="{{avatar}}" class="avatar_image">
        {{else}}
            <img src="{{default_avatar}}" class="avatar_image">
        {{/if}}
    </div>

    {{#if label}}<div class="avatar_label">{{label}}</div>{{/if}}
</div>